# Copy to .env and set values. Do not commit .env.

# Server
NODE_ENV=production
PORT=3000

# Production: set a long random string. Admin endpoints require header: X-Admin-Key: <value>
# Leave empty in development to allow unauthenticated admin access.
ADMIN_SECRET=

# Production: allowed origin for CORS (e.g. https://serenest.in). Leave empty to allow all.
ALLOWED_ORIGIN=
