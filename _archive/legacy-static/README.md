# Legacy static pages (do not serve)

Pre-React marketing HTML that previously lived at the repo root.

Vite maps `screening.html` → `/screening`, which **shadowed** the React Mental Health Center during local development.

Keep these files only for historical reference. Do not move them back to the project root.
