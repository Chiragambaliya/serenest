import React from 'react';
import CheckLandingPage from './CheckLandingPage';

/** Burnout Check product landing — thin route wrapper over reusable CheckLandingPage. */
export default function BurnoutCheckLandingPage() {
  return <CheckLandingPage toolId="bat12" />;
}
