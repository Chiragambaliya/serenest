// ——— Backend URL (required if admin/booking get "Could not remove" or "Network error") ———
// Paste your backend URL below (no trailing slash). Get it from: Render Dashboard → your service → copy the URL.
window.API_BASE = 'https://serenest.onrender.com';

// WhatsApp contact number (digits only, with country code, no + or spaces).
window.WHATSAPP_NUMBER = '917777936367';
